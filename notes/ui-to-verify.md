# com.rubickanov.ui: что осталось

Аудит 2026-09-24. Всё остальное из прежней версии заметки исправлено в 3.0.0.

## Не покрыто тестами

- `RunFollowLoop` в `PopupHost`: что исключение одного попапа не останавливает остальных. Нужен PlayMode-тест с
  PlayerLoop.
- `PopupManipulator` с задержкой больше нуля: показ идёт через планировщик панели, в EditMode-тесте его время не
  идёт. Наведение и уход без задержки покрыты в `LivePanelTests`.

## Решено не делать

- **`SpinnerHost` как пресет попапа.** Спиннер один на все хэндлы и показывает метку последнего, попап один на хэндл.
  Пресет потребовал бы того же счётчика хэндлов поверх попапа, выигрыша нет.
