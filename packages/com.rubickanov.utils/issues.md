# Audit findings — com.rubickanov.utils

Generated by automated review.

## Minor (conventions / shortcomings)

- [x] **`EvictingPool` constructor does not validate `maxActive`** — `Runtime/Unity/EvictingPool.cs:45-55` — The sibling `ObjectPool` validates every constructor argument (`prefab`, `prewarm`, `maxSize`) and throws `ArgumentNullException` / `ArgumentOutOfRangeException` at the API boundary, but `EvictingPool` validated none of its own parameters. With `maxActive <= 0`, the first `Get()` (line 60/75) sees `_active.Count >= _maxActive` as immediately true and calls `EvictOldest()`, where `_active.First!.Value` (line 124) dereferences a null `LinkedListNode<T>` and throws a confusing `NullReferenceException` instead of a clear `ArgumentOutOfRangeException` at construction. Fix: add `if (maxActive <= 0) throw new ArgumentOutOfRangeException(nameof(maxActive), "maxActive must be positive.");` (and optionally guard `evictBuffer < 0`) in the constructor. Fixed: ctor now throws `ArgumentOutOfRangeException` for non-positive `maxActive` and negative `evictBuffer`; regression tests cover both.
