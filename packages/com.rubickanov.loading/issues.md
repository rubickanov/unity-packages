# Audit findings — com.rubickanov.loading

Generated by automated review.

## Minor (conventions / shortcomings)

- **README presenter example does not compile against `ILoadingPresenter`** — `README.md:92-105` — The `UILoadingPresenter` sample in "Implementing a Presenter" declares `public void Hide() => _ui.Hide<LoadingScreen>();`, but the interface (`Runtime/ILoadingPresenter.cs:39`) declares `UniTask Hide()`. The example also omits `UniTask WaitForInput(CancellationToken ct = default)` entirely (`Runtime/ILoadingPresenter.cs:32`). As written the class would not satisfy the interface and would fail to compile, which violates the README standard's rule that code examples must work against the actual API. Fix: make `Hide()` return `UniTask` and add a `WaitForInput` implementation (e.g. returning `UniTask.CompletedTask` or awaiting an input gate).
