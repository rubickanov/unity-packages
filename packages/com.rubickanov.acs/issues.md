# Audit findings — com.rubickanov.acs

Generated by automated review.

## Minor (conventions / shortcomings)

- **README documents a non-existent `OnAwake` override (won't compile)** — `README.md:170-177` — The "Component Lifecycle" section instructs the reader to "override `OnAwake` — never `Awake` itself" and shows `protected override void OnAwake() { ... }`. `EntityComponent` (`Runtime/Unity/Behavior/EntityComponent.cs`) has no `OnAwake` member, so that example fails to compile (`CS0115: no suitable method found to override`). The actual extension path is to override the `virtual Awake()` and call `base.Awake()` (which is what the test `Tests/Unity/Behavior/EntityComponentTests.cs:135` does). Either add an `OnAwake` hook invoked from `Awake()` after injection, or change the README to document overriding `Awake()` + `base.Awake()`.

- **`EntityComponent` XML/inline docs contradict the actual code (broken cref, false "non-virtual" claim)** — `Runtime/Unity/Behavior/EntityComponent.cs:13-16,36-41` — The class summary says "`Awake` is intentionally non-virtual … To run logic at Awake time, override `<see cref="OnAwake"/>` instead — it is invoked after injection", and the inline comment at line 37-39 claims "The C# compiler now rejects `override void Awake` on any subclass". Both are false: line 41 declares `protected virtual void Awake()`, so subclasses can (and the test does) override it, and there is no `OnAwake` method, so the `<see cref="OnAwake"/>` is a dangling doc reference. The docs describe a design that was reverted; align them with the real `virtual Awake()` contract (or reintroduce the non-virtual `Awake` + `OnAwake` hook the docs and the README both assume).
