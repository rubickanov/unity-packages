# Audit findings — com.rubickanov.localization

Generated by automated review.

## Minor (conventions / shortcomings)

- **Dead variable `needsTableConst`** — `Editor/LocalizationKeysGenerator.cs:147` — `needsTableConst` is computed (including a non-trivial LINQ expression over child nodes) but never read; the actual `Table` const emission on line 148 uses a different condition (`node.Leaves.Count > 0 || node.Children.Count == 0`). This is dead code that looks like a leftover from an earlier version of the const-emission logic. Either remove the variable or restore it as the gating condition if that was the intent.

- **`default(LangLocale)` is not equal to `LangLocale.Empty`** — `Runtime/LangLocale.cs:41,45` — `Empty` is constructed with `string.Empty`, but `default(LangLocale)` has a `null` `Code`. `Equals` does `string.Equals(Code, other.Code, StringComparison.OrdinalIgnoreCase)`, and `string.Equals(null, "", ...)` returns `false`, so `LangLocale.Empty != default(LangLocale)` even though both report `IsEmpty == true` and hash to the same value (collision without equality). This is a subtle equality foot-gun: two semantically-empty locales compare unequal. Normalize `Code` in comparisons (e.g. coalesce to `string.Empty` in `Equals`, mirroring `GetHashCode`) so all empty locales are equal.
