# Audit findings — com.rubickanov.ui.localization

Generated by automated review.

## Critical (correctness / bugs)

- **UIToolkit assembly is missing the R3 reference** — `UIToolkit/UI.Localization.UIToolkit.asmdef:4-11` — The asmdef references UI.Runtime (`83ac5b11`), UI.UIToolkit (`345547c0`), UI.Localization.Runtime (`99f3ec1a`), Localization.Runtime (`68edc639`), UniTask (`77221876`) and one more external GUID (`eec0964c`, Unity.Localization for the `Locale` type), but it does **not** reference R3 (`f51ebe6a` — the GUID confirmed from `com.rubickanov.ui/UIToolkit/UI.UIToolkit.asmdef` and `UI.Runtime.asmdef`). The extension code directly names R3 types: `UIToolkitLocalizationExtensions.cs:45/69/92/143` pass `loc.OnLocaleChanged` (type `R3.Observable<Locale>`) and `UIToolkitLocalizationExtensions.cs:168` passes `loc.IsRTL` (type `R3.ReadOnlyReactiveProperty<bool>`, which must upcast to `Observable<bool>`) into `BindObservable<T>(Observable<T>, …)`. Unity asmdef references are not transitive, so consuming R3 types without a direct reference produces CS0012 ("the type is defined in an assembly that is not referenced") and the assembly fails to compile. Fix: add `"GUID:f51ebe6a0ceec4240a699833d6309b23"` to the references array.

## Minor (conventions / shortcomings)

- **Unused UniTask dependency advertised in README and Runtime asmdef** — `README.md:10`, `Runtime/UI.Localization.Runtime.asmdef:7` — The README lists "`UniTask` — async/await" as a dependency and the Runtime asmdef references UniTask (`77221876`), but neither `LocalizationViewExtensions.cs` nor `UIToolkitLocalizationExtensions.cs` contains any async/await or UniTask usage. The reference is dead and the README claim is misleading. Drop the UniTask line from the README (or restate why it is needed) and remove the unused reference from the Runtime asmdef.
</content>
</invoke>
