# Audit findings — com.rubickanov.steam-transport

Generated by automated review.

## Minor (conventions / shortcomings)

- [x] **Missing `Runtime/csc.rsp` while source relies on nullable context** — `Runtime/SteamNetworkingSocketsTransport.cs:24`, `:96`, `:397` — *(Resolved: `Runtime/csc.rsp` is present with `-nullable:enable` / `-langVersion:10`, matching siblings.)* — The file uses nullable reference annotations (`Callback<SteamNetConnectionStatusChangedCallback_t>?` at line 24, `NetworkManager? networkManager` at line 397) and the null-forgiving operator (`payload.Array!` at line 96). Every other package in the monorepo ships a `Runtime/csc.rsp` containing `-nullable:enable` and `-langVersion:10` (e.g. `com.rubickanov.acs.netcode/Runtime/csc.rsp`), but this package has none. Without `-nullable:enable`, each `?` annotation on a reference type emits compiler warning CS8632 and the annotations carry no analysis meaning. Fix: add `Runtime/csc.rsp` matching the sibling packages.

- [x] **`StartServer` never calls `InitRelayNetworkAccess()`** — `Runtime/SteamNetworkingSocketsTransport.cs:34-48` — *(Fixed: `StartServer` now calls `InitRelayNetworkAccess()` before opening the listen socket, gated by `UNITY_SERVER` like `StartClient`.)* — `StartClient` initializes relay network access (lines 57-61) before `ConnectP2P`, but `StartServer` creates the P2P listen socket without it. Steam's relay network configuration is fetched asynchronously; initializing it up front on the server/host side too avoids the first incoming connections stalling while the relay network comes online. Fix: call `InitRelayNetworkAccess()` (gated by `UNITY_SERVER` like the rest) at the top of `StartServer`.

- [x] **`UnreliableSequenced` mapping does not preserve sequencing** — `Runtime/SteamNetworkingSocketsTransport.cs:89` — *(Documented: caveat comment added at the mapping noting Steam has no unreliable-sequenced primitive and NoNagle does not drop stale/out-of-order packets. README delivery-table note still pending under P4 doc work.)* — `NetworkDelivery.UnreliableSequenced` maps to `k_nSteamNetworkingSend_UnreliableNoNagle`. NoNagle only disables Nagle batching; it does not drop stale/out-of-order packets, so NGO's "sequenced" contract (newer packet supersedes older) is not honored and old packets can be applied after newer ones. Steam has no unreliable-sequenced primitive, so this is a real limitation rather than a trivial fix — at minimum it should be documented as a caveat in the README delivery table (which currently presents the mapping as exact).
